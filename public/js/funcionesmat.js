
metodos = {
    init: function () {

    },
    Funciones: {
        suma: function (a, b) {
            var result = a + b;
            return result;
        }
    },


    function: {
        resta: function (q, l) {
            var rest = q - l;
            return rest;
        }
    },

    function: {
        multiplicar: function (e, r) {
            var mult = e * r;
            return mult;
        }
    },

    function: {
        dividir: function (g, m) {
            var div = g / m;
            return div;
        }
    },


    function: {
        persona: function (a, b, c, d, e) {
            var acciones = a + " " + b + " " + c + " " + d + " " + e;
            return acciones;
        }
    }
}
